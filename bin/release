#!/usr/bin/env bash
# bin/release <build-dir>

BUILD_DIR=${1:-}

if [ -f $BUILD_DIR/.builddir ]; then
  BUILD_DIR=$BUILD_DIR`cat $BUILD_DIR/.builddir`
fi

cat << EOF
addons: []
default_process_types:
  web: npm start
EOF
